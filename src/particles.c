#include "particles.h"

#define MAX_PARTICLES 100

static int index;
static int count;
static struct Particle pool[MAX_PARTICLES];

void particles_spawn(int x, int y, float rotation) {
  
}

void particles_update() {
  
}

void particles_draw(PlaydateAPI* playdate) {
  
}